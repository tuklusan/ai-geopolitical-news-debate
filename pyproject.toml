[project]
name = "code-workspace"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "aiohttp>=3.9,<4.0",
    "aiohttp-cors>=0.7",
    "beautifulsoup4>=4.12",
    "lxml>=5.0",
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "python-dotenv>=1.0",
    "pyyaml>=6.0.3",
]
